# Runtime dependencies for the CrewAI Agent Designer (CML AMP + local).
# Pin major/minor for reproducible CML installs; bump intentionally.

streamlit>=1.36,<2
crewai>=1.15,<2
crewai-tools>=1.15,<2
pydantic>=2.7,<3
pyyaml>=6.0,<7
jinja2>=3.1,<4
httpx>=0.27,<1

# Visual canvas — streamlit-flow-component is primary; agraph is fallback.
streamlit-flow-component>=1.5,<2
streamlit-agraph>=0.0.45,<1
