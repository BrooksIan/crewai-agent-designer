# {{ crew.name }}

Generated by the [CrewAI Agent Designer](https://github.com/cloudera/crewai-agent-designer).

## Contents

```
{{ crew.name | lower }}/
├── config/
│   ├── agents.yaml   # {{ agents | length }} agent(s)
│   └── tasks.yaml    # {{ tasks | length }} task(s)
├── crew.py           # Class {{ crew.name }} — Process.{{ crew.process }}
├── requirements.txt
└── README.md
```

## Run it

```bash
pip install -r requirements.txt
export OPENAI_API_KEY="…"   # or your configured LLM provider
python crew.py
```

## Re-generate

This project was exported from a saved design in the Agent Designer. To
change anything, re-open the design in the designer, edit, and re-export —
or hand-edit these files if you've forked the project.
